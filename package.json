{
  "name": "workshop-2026",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "openapi:types": "pnpm --filter @workshop/types run generate",
    "dev": "turbo run dev --parallel",
    "build": "pnpm openapi:types && turbo run build",
    "lint": "turbo run lint",
    "typecheck": "pnpm openapi:types && turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
